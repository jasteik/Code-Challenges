# Write a function that takes in a string of one or more words, and returns the same string, 
#but with all five or more letter words reversed (Just like the name of this Kata). Strings 
#passed in will consist of only letters and spaces. Spaces will be included only when more 
#than one word is present.

def spin_words(sentence):
    # Create a list that I can append items to then join back again at the end
    list = []
    # Create for loop to split each word in sentence
    for x in sentence.split():
        # Create if else to test if the length of word is 5 or more
        if len(x) >= 5:
            # if length is 5 or more reverse the word
            x = x[::-1]
            # Now append word to the list.
            list.append(x)
        else:
            # The words less than 5 need to be put into the list so words read in order.
            list.append(x)
    # Return the string as a string and not a list by using spaces betweeen words and
    # Joining the words in the list into a string.
    print(' '.join(list))
   

spin_words('This is the warriors time to shine brightly')
